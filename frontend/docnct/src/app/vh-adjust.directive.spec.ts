import { VhAdjustDirective } from './vh-adjust.directive';

describe('VhAdjustDirective', () => {
  it('should create an instance', () => {
    const directive = new VhAdjustDirective();
    expect(directive).toBeTruthy();
  });
});
